import { Box } from '@mui/material';

function Homepage() {
  return (
    <Box bgcolor={'white'} flex={1} flexGrow={1} display={'flex'}>
      <h1>Homepage</h1>
    </Box>
  )
}

export default Homepage